{
  "name": "hyperapp",
  "description": "1 KB JavaScript library for building frontend applications.",
  "version": "0.12.0",
  "main": "dist/hyperapp.js",
  "jsnext:main": "src/index.js",
  "module": "src/index.js",
  "license": "MIT",
  "repository": "hyperapp/hyperapp",
  "typings": "hyperapp.d.ts",
  "files": [
    "src",
    "dist"
  ],
  "author": "Jorge Bucaran",
  "keywords": [
    "hyperapp",
    "react",
    "elm",
    "jsx",
    "virtual dom",
    "vdom",
    "hyperx",
    "hyperscript"
  ],
  "scripts": {
    "test": "npm run -s test:unit && npm run -s test:dts",
    "test:unit": "jest --coverage --no-cache",
    "test:dts": "typings-checker --project test/dts/tsconfig.json --allow-expect-error test/dts/**.tsx",
    "build": "npm run bundle && npm run minify",
    "bundle": "rollup -i src/index.js -o dist/hyperapp.js -m -f umd -n hyperapp",
    "minify": "uglifyjs dist/hyperapp.js -o dist/hyperapp.js --mangle --compress warnings=false --pure-funcs=Object.defineProperty -p relative --source-map dist/hyperapp.js.map",
    "prepublish": "npm run build",
    "format": "prettier --semi false --write 'src/**/*.js' 'hyperapp.d.ts'",
    "release": "npm run build && npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "babel": {
    "presets": "es2015"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "jest": "^20.0.4",
    "prettier": "~1.6.0",
    "rollup": "^0.49.1",
    "typescript": "^2.4.2",
    "typings-checker": "^2.0.0",
    "uglify-js": "^2.7.5"
  }
}
